// Create an IIFE(Immediately Invoked Function Expression) that calculates the square of a number and
// immediately displays the result.
(function (number) {
    console.log(`Squre of ${number} is ${number * number}`);
}(5));